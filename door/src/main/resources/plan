用户:user

//项目:project
楼栋:build(id,name,code,createTime,updateTime)
单元:unit
房屋:house
//房间:room

门锁:lock(id,houseId,name)
网关:gateway(id,unitId,uuid,name,coordinate/*build-unit*/,version,ip,port,remote)

gateway-lock(id,gatewayId,lockId)==>1:n

gateway-record

lock-record

状态:(lock)status(id,lockId,type,flag);